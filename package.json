{
  "name": "funey",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p $PORT",
    "db:up": "initdb -D .db && pg_ctl -D .db -l logfile start && createdb funey && psql funey < schema.dump",
    "db:down": "pg_ctl -D .db stop && rm -rf .db"
  },
  "dependencies": {
    "bootstrap": "^5.3.2",
    "moment": "^2.29.4",
    "next": "^13.4.0",
    "pg": "^8.11.1",
    "react": "^18.2.0",
    "react-bootstrap": "^2.8.0",
    "react-dom": "^18.2.0",
    "sha256": "^0.2.0"
  }
}
